import styled from "styled-components"

export const LayoutStyles = styled.div`
  display: flex;
  flex-direction: column;
  min-height: 100vh;

  main {
    flex-grow: 1;
    flex-shrink: 1;
    flex-basis: auto;
  }
`
