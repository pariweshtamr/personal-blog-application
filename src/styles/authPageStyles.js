import styled from "styled-components"

export const AuthPageStyles = styled.div`
  .nav-tabs {
    --bs-nav-tabs-link-active-bg: var(--bg);
    --bs-nav-tabs-link-active-color: #000;
    --bs-nav-tabs-link-active-border-color: #dee2e6 #dee2e6 var(--main-color);
    li {
      background: #fff;
      button {
        color: var(--text-color);
      }
    }
  }
`
